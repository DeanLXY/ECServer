<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace :命名空间需要 接口的全限定名 #{version} 占位符 , version随意些 -->
<mapper namespace="zz.itcast.ecserver.dao.IOrderDao">

	<select id="getOrderListByUserId" resultType="OrderInfo"
		parameterType="int">

		SELECT id
		order_id,user_id,address_id,delivery_type,payment_type,invoice_type,invoice_title invoiceTitle,invoice_content invoiceContent,STATUS,price,time,flag
		FROM orderlist WHERE user_id=#{0} and type=#{1}
	</select>

</mapper>